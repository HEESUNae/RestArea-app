import styled from 'styled-components';

export const StyledHeader = styled.header`
  background-color: #153448;
  .inner {
    padding: 2rem;
  }
  .logo {
    a {
      font-size: 1.8rem;
      font-weight: 600;
      color: #fff;
    }
  }
`;
